include:
  - infra.yaml
services:
  back:
    container_name: back
    build: .
    ports: 
      - "8000:8000"
    develop:
      watch:
        - action: sync
          path: .
          target: /app